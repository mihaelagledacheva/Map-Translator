#include "contouring.h"
#include <opencv2/imgproc.hpp>

void contouring::contour(cv::Mat *image, std::vector<std::vector<cv::Point>> *contours)
{
    // cv::findContours(image, contour, cv::RETR_EXTERNAL, cv::CHAIN_APPROX_SIMPLE);
}
